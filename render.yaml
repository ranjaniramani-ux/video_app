services:
  - type: web
    name: language-video-app
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y ffmpeg imagemagick
    startCommand: python server.py